# ─────────────────────────────────────────────────────────────────────────────
# BitGridAI — Runtime-Abhängigkeiten
#
# Nur das, was im laufenden System (Container / Produktion) gebraucht wird.
# Für Entwicklung: pip install -r requirements-dev.txt
# ─────────────────────────────────────────────────────────────────────────────

# Datenmodelle & Validierung (src/core/models.py, src/adapters/)
pydantic>=2.0

# YAML-Konfiguration (rules, config-Dateien)
pyyaml>=6.0

# .env laden (alle Adapter, src/main.py)
python-dotenv>=1.0

# MQTT-Client (src/adapters/mqtt_client.py → Broker-Kommunikation)
paho-mqtt>=1.6

# Modbus TCP (src/adapters/modbus_adapter.py → Batterie-BMS)
pymodbus>=3.5

# REST API (src/ui/ → Web-Interface & interne API)
fastapi>=0.110
uvicorn[standard]>=0.29

# SQLite-Zugriff (src/data/event_store.py, state_store.py)
sqlite-utils>=3.35
