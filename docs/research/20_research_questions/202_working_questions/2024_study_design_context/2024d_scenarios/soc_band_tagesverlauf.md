# SoC-Band-Szenarien (reales 10-kWh-System) mit Erklärtexten A/B

**Stand:** 2026-06-22 · **Kern:** `strategy="soc_band"` (Produktiv-Steuerung nachgebildet)

**Konstruierte Einzel-Snapshots, kein durchgehender Tag.** Jede Zeile ist ein unabhängiger Zustand, der einzeln gegen den Kern verifiziert wurde (Engine-Output == erwarteter `decision_code`, 13/13). Sie sind entlang eines plausiblen Tages angeordnet, aber **nicht** energiebilanziert: am realen 10-kWh-Speicher durchläuft der Akku den Bandanstieg in ~40 min (Replay: `python -m src.sim.soc_band_day --season sommer --start-soc 21`), die Bänder wären in einem echten Verlauf also nicht über den Tag verteilt. Für eine Verständlichkeitsstudie zählt die zeilen-interne Stimmigkeit (Datenbasis ↔ Code), und die ist belegt.

Verifikations-Config: `RuleEngineConfig(strategy="soc_band", pv_start_w=6000, max_grid_import_w=500, max_chip_temp_c=112, night_block_enabled=True)`.

Parameter: Stop 50 %, Eco-Start 58 %, Standard 80 % (Stop 75 %), Super 90 % (Stop 85 %), Eco-Frischstart braucht PV ≥ 6,0 kW, Sicherheitsgrenze Temperatur 112 °C, Netzgrenze 500 W, Nachtsperre 22:00–06:00. Sommertag, **10-kWh-Speicher**, über Nacht entladen.

**Text A (Gruppe A, statisch/regelbasiert):** vollständige, neutrale Prosa im System-Ton, deterministisch aus dem `decision_code` ableitbar (Auslöser, Datenbasis mit Werten und Schwellen, Folge, Änderungsbedingung). **Text B (Gruppe B, LLM):** **derselbe Informationsgehalt** (inklusive Änderungsbedingung), aber als **BitHamster** erzählt, dem Maskottchen und KI-Energieberater der App: ein ruhiger, schlauer Krypto-Hamster mit AR-Brille und Hardware-Wallet in den Pfoten, der in Ich-Form, warm und in Alltagssprache die komplexe Automation für Laien runterbricht (sein Zustand spiegelt die Anzeige: er ruht/döst bei Stopp, hält geduldig die Stellung bei Pause, minet grün im Eco-Modus und legt an Leistung zu bis zur vollen Super-Last).

Beide Bedingungen sind bewusst **Prosa** und **informationsgleich**, sodass sich A und B nur in der **Stimme** unterscheiden (Regel-Template vs. LLM), nicht im **Format** (Stichwortliste vs. Satz) und nicht im **Informationsgehalt** (vollständig vs. verdichtet). Sonst wäre ein gemessener Vertrauensunterschied (H1) nicht interpretierbar, weil die Quelle mit Format oder Vollständigkeit konfundiert wäre. A ist bewusst eine faire Erklärung (kein Strohmann); Werte, Schwellen und R1–R5 sind in A und B identisch (Prereg [2024a](../2024a_study_design_sampling.md), [2024e](../2024e_erkenntnisziele_prereg.md)).

Die Spalte **Text A (Platzhalter)** zeigt dieselbe Erklärung mit den Entity-/Config-Platzhaltern (`DecisionEvent.params`, wie in `text_blocks.yaml`) statt konkreter Werte; die Bindung an die HA-Entities steht in der Legende unter der Tabelle.

| Uhrzeit | Szenario (Code) | Text A (statisch, regelbasiert) | Text A (Platzhalter / Entities) | Text B (LLM, BitHamster) |
|---|---|---|---|---|
| 06:00 | Morgentief (`STOP_R1_SOC_RESERVE_STOP`) | Der Ladezustand ist über Nacht auf 21 % gefallen, unter die Hausreserve von 50 %.<br>Der Miner bleibt aus und lädt zuerst wieder auf.<br>Ein Start ist erst ab 58 % möglich. | Der Ladezustand ist über Nacht auf `{battery_soc_pct}` % gefallen, unter die Hausreserve von `{soc_stop_pct}` %.<br>Der Miner bleibt aus und lädt zuerst wieder auf.<br>Ein Start ist erst ab `{soc_eco_start_pct}` % möglich. | Ich döse noch: über Nacht ist dein Speicher auf 21 % gesunken, unter die 50 %, die ich fürs Haus frei halte.<br>Ab 58 % wache ich auf und mine weiter. |
| 09:00 | Eco-Band, Sonne schwach (`NOOP_R1_SOC_HOLD_PV`) | Der Ladezustand liegt mit 60 % im Eco-Band, doch die PV reicht mit 3,2 kW nicht (nötig 6,0 kW).<br>Der Miner bleibt aus.<br>Ab 6,0 kW startet er im Eco-Modus. | Der Ladezustand liegt mit `{battery_soc_pct}` % im Eco-Band, doch die PV reicht mit `{pv_power_w}` kW nicht (nötig `{pv_start_w}` kW).<br>Der Miner bleibt aus.<br>Ab `{pv_start_w}` kW startet er im Eco-Modus. | Ich halte geduldig die Stellung: dein Speicher (60 %) wäre bereit, aber zum Loslegen fehlt mir Sonne, 3,2 kW sind da, ich brauche 6,0 kW.<br>Ab 6,0 kW lege ich los. |
| 10:30 | Frischstart Eco (`THROTTLE_R1_ECO`) | Der Ladezustand liegt mit 64 % im Eco-Band, und die PV erreicht mit 6,3 kW die Frischstart-Schwelle (6,0 kW).<br>Der Miner startet gedrosselt im Eco-Modus.<br>Auf Standard wechselt er ab 80 %. | Der Ladezustand liegt mit `{battery_soc_pct}` % im Eco-Band, und die PV erreicht mit `{pv_power_w}` kW die Frischstart-Schwelle (`{pv_start_w}` kW).<br>Der Miner startet gedrosselt im Eco-Modus.<br>Auf Standard wechselt er ab `{soc_standard_pct}` %. | Genug Sonne (6,3 kW), jetzt mine ich grün und sparsam im Eco-Modus, ganz aus deinem Solarüberschuss.<br>Steigt dein Speicher über 80 %, schalte ich eine Stufe höher (Standard). |
| 11:30 | Hochschalten Standard (`START_R1_STANDARD`) | Der Ladezustand erreicht mit 80 % das Standard-Band.<br>Der Miner wechselt vom Eco- in den Standard-Modus.<br>Ab 90 % folgt Super, unter 75 % fällt er auf Eco zurück. | Der Ladezustand erreicht mit `{battery_soc_pct}` % das Standard-Band.<br>Der Miner wechselt vom Eco- in den Standard-Modus.<br>Ab `{soc_super_pct}` % folgt Super, unter `{soc_standard_stop_pct}` % fällt er auf Eco zurück. | Dein Speicher ist auf 80 % geklettert, jetzt fahre ich die Leistung auf Standard hoch.<br>Ab 90 % gehe ich auf volle Last, unter 75 % drossle ich zurück auf Eco. |
| 12:00 | Stufe halten (`NOOP_R5_MIN_RUNTIME_NOT_REACHED`) | Ein Moduswechsel käme zu früh: Standard läuft erst seit einem von drei Pflicht-Intervallen (je 10 Minuten).<br>Die Stufe wird gehalten, um häufiges Umschalten zu vermeiden.<br>Ein Wechsel ist frühestens nach drei Intervallen möglich. | Ein Moduswechsel käme zu früh: Standard läuft erst seit `{blocks_since_last_change}` von `{min_runtime_blocks}` Pflicht-Intervallen (je 10 Minuten).<br>Die Stufe wird gehalten, um häufiges Umschalten zu vermeiden.<br>Ein Wechsel ist frühestens nach `{min_runtime_blocks}` Intervallen möglich. | Ich bleibe noch auf Standard: mein letzter Wechsel ist erst ein Intervall her, sonst schalte ich ständig um.<br>Erst nach drei Intervallen (rund 30 Minuten) wechsle ich wieder. |
| 13:00 | Hochschalten Super (`START_R1_SUPER`) | Der Ladezustand erreicht mit 91 % das Super-Band (ab 90 %).<br>Der Miner wechselt von Standard auf Super mit voller Last.<br>Unter 85 % fällt er auf Standard zurück. | Der Ladezustand erreicht mit `{battery_soc_pct}` % das Super-Band (ab `{soc_super_pct}` %).<br>Der Miner wechselt von Standard auf Super mit voller Last.<br>Unter `{soc_super_stop_pct}` % fällt er auf Standard zurück. | Voll geladen, dein Speicher ist über 90 %, jetzt mine ich mit voller Leistung (Super).<br>Fällt er unter 85 %, schalte ich zurück auf Standard. |
| 13:30 | Übertemperatur (`STOP_R3_OVERTEMP`) | Die Chip-Temperatur liegt mit 113 °C über der Sicherheitsgrenze von 112 °C.<br>Der Miner wird sofort gestoppt, nicht überstimmbar.<br>Ein Wiederanlauf erfolgt erst nach Abkühlung. | Die Chip-Temperatur liegt mit `{miner_temp_c}` °C über der Sicherheitsgrenze von `{max_chip_temp_c}` °C.<br>Der Miner wird sofort gestoppt, nicht überstimmbar.<br>Ein Wiederanlauf erfolgt erst nach Abkühlung. | Beim Minen wird mir mit 113 °C zu heiß, über der Sicherheitsgrenze von 112 °C, darum schalte ich sofort ab.<br>Erst wenn ich abgekühlt bin, geht's weiter. |
| 14:30 | Wolke, Netzbezug (`STOP_R2_GRID_IMPORT_EXCEEDED`) | Eine Wolke senkt die PV auf 0,8 kW, der Miner zöge 700 W aus dem Netz (erlaubt 500 W).<br>Er wird gestoppt, damit nicht mit Netzstrom gemined wird.<br>Start wieder ab Netzbezug unter 500 W. | Eine Wolke senkt die PV auf `{pv_power_w}` kW, der Miner zöge `{grid_import_w}` W aus dem Netz (erlaubt `{max_grid_import_w}` W).<br>Er wird gestoppt, damit nicht mit Netzstrom gemined wird.<br>Start wieder ab Netzbezug unter `{max_grid_import_w}` W. | Eine Wolke dämpft die Sonne, jetzt zöge ich 700 W aus dem Netz, erlaubt sind nur 500 W, also lege ich die Mine still.<br>Ab Netzbezug unter 500 W mache ich weiter. |
| 16:00 | Akku reicht, Sonne nicht (`NOOP_R1_SOC_HOLD_PV`) | Der Ladezustand reicht mit 78 % aus, doch die PV liegt mit 2,2 kW unter der Frischstart-Schwelle (6,0 kW).<br>Der Miner bleibt aus.<br>Er startet, sobald die PV 6,0 kW erreicht. | Der Ladezustand reicht mit `{battery_soc_pct}` % aus, doch die PV liegt mit `{pv_power_w}` kW unter der Frischstart-Schwelle (`{pv_start_w}` kW).<br>Der Miner bleibt aus.<br>Er startet, sobald die PV `{pv_start_w}` kW erreicht. | Ich halte die Stellung: dein Speicher (78 %) würde reichen, aber 2,2 kW Sonne sind mir zu wenig zum Loslegen, ich brauche 6,0 kW.<br>Ab 6,0 kW mine ich wieder. |
| 17:30 | Später Nachmittag (`NOOP_R1_SOC_HOLD_PV`) | Der Ladezustand liegt mit 72 % im Eco-Band, doch 1,5 kW PV reichen nicht zum Anlaufen (nötig 6,0 kW).<br>Der Miner bleibt aus.<br>Er startet ab 6,0 kW PV-Leistung. | Der Ladezustand liegt mit `{battery_soc_pct}` % im Eco-Band, doch `{pv_power_w}` kW PV reichen nicht zum Anlaufen (nötig `{pv_start_w}` kW).<br>Der Miner bleibt aus.<br>Er startet ab `{pv_start_w}` kW PV-Leistung. | Noch halte ich still: dein Speicher liegt bei 72 %, aber 1,5 kW Sonne reichen mir nicht zum Loslegen.<br>Erst ab 6,0 kW lege ich wieder los. |
| 20:00 | Abend, Akku sinkt (`NOOP_R1_SOC_HOLD`) | Der Ladezustand ist abends auf 55 % gesunken, ins Halteband unter der Eco-Startgrenze (58 %).<br>Der Miner bleibt aus.<br>Der Reserveschutz greift ab 50 %. | Der Ladezustand ist abends auf `{battery_soc_pct}` % gesunken, ins Halteband unter der Eco-Startgrenze (`{soc_eco_start_pct}` %).<br>Der Miner bleibt aus.<br>Der Reserveschutz greift ab `{soc_stop_pct}` %. | Am Abend ist dein Speicher auf 55 % gesunken, knapp unter meine Startgrenze von 58 %, also lege ich eine Pause ein.<br>Ab 50 % halte ich ohnehin die Reserve fürs Haus frei. |
| 21:00 | Hausreserve (`STOP_R1_SOC_RESERVE_STOP`) | Der Ladezustand ist mit 48 % unter die Hausreserve von 50 % gefallen.<br>Der Miner wird gestoppt, um die Reserve fürs Haus freizuhalten.<br>Ein Start ist erst ab 58 % wieder möglich. | Der Ladezustand ist mit `{battery_soc_pct}` % unter die Hausreserve von `{soc_stop_pct}` % gefallen.<br>Der Miner wird gestoppt, um die Reserve fürs Haus freizuhalten.<br>Ein Start ist erst ab `{soc_eco_start_pct}` % wieder möglich. | Ich rolle mich zusammen: dein Speicher ist auf 48 % gefallen, ab 50 % halte ich die Reserve fürs Haus frei.<br>Erst ab 58 % wache ich wieder auf. |
| 22:00 | Nachtsperre (`NOOP_NIGHT_BLOCK`) | Es ist 22:00 Uhr, Beginn des Nachtfensters von 22:00 bis 06:00 Uhr.<br>In dieser Zeit ist das Mining unabhängig vom Ladezustand ausgesetzt.<br>Der Normalbetrieb beginnt wieder ab 06:00 Uhr. | Es ist `{hour}`:00 Uhr, Beginn des Nachtfensters von `{night_start_hour}`:00 bis `{night_end_hour}`:00 Uhr.<br>In dieser Zeit ist das Mining unabhängig vom Ladezustand ausgesetzt.<br>Der Normalbetrieb beginnt wieder ab `{night_end_hour}`:00 Uhr. | Nachtruhe! Zwischen 22 und 6 Uhr rolle ich mich ein und ruhe, egal wie voll dein Speicher ist.<br>Ab 6 Uhr bin ich wieder für dich da. |

### Platzhalter-Legende (Entity-/Config-Bindung)

| Platzhalter | Quelle (HA-Entity / Config) | Wert |
|---|---|---|
| `{battery_soc_pct}` | `sensor.battery_soc_pct` | Messwert |
| `{pv_power_w}` | `sensor.pv_power_w` | Messwert |
| `{grid_import_w}` | `sensor.grid_import_w` | Messwert |
| `{miner_temp_c}` | `sensor.miner_max_chip_temp_c` | Messwert |
| `{soc_stop_pct}` | `input_number.mvp_soc_stop_pct` | 50 (Hausreserve) |
| `{soc_eco_start_pct}` | `input_number.mvp_soc_eco_start_pct` | 58 |
| `{soc_standard_pct}` | `input_number.mvp_soc_std_pct` | 80 |
| `{soc_standard_stop_pct}` | `input_number.mvp_soc_std_stop_pct` | 75 (Hysterese, noch nicht in `params`) |
| `{soc_super_pct}` | `input_number.mvp_soc_super_pct` | 90 |
| `{soc_super_stop_pct}` | `input_number.mvp_soc_super_stop_pct` | 85 (Hysterese, noch nicht in `params`) |
| `{pv_start_w}` | `RuleEngineConfig.pv_start_w` | 6 000 W (Eco-Frischstart, Labor-skaliert) |
| `{max_grid_import_w}` | `input_number.r2_max_grid_import_w` | 500 |
| `{max_chip_temp_c}` | `input_number.mvp_tmax_std_threshold_c` | 112 |
| `{blocks_since_last_change}` / `{min_runtime_blocks}` | R5-Zähler / `RuleEngineConfig` | — / 3 |
| `{hour}` / `{night_start_hour}` / `{night_end_hour}` | Blockzeit / `RuleEngineConfig` | — / 22 / 6 |

## Hinweise zur Kern-Angleichung

- Alle 13 Snapshots gegen den Kern verifiziert; Belege in `tests/core/test_soc_band_strategy.py`. Tagesbogen reproduzierbar über `src/sim/soc_band_day.py` (geschlossener Regelkreis, 10 kWh).
- **Format-, Informations- und Längen-Angleich (2026-06-22):** Text A liegt als vollständige neutrale Prosa vor (nicht mehr im `·`-Dashboard-Format); Text B trägt **denselben Informationsgehalt** (inklusive Änderungsbedingung) in der **BitHamster-Stimme** (Ich-Form, KI-Energieberater für Laien); und beide sind auf **vergleichbare Länge** gebracht (A und B je ~30 Wörter, kurze Sätze mit `<br>`-Zeilenumbruch). Damit variiert zwischen A und B nur die **Stimme** (neutraler Regeltext vs. BitHamster-LLM), nicht Format, Vollständigkeit oder Länge, sonst wäre H1 konfundiert. A bleibt eine faire Erklärung (kein Strohmann). Beim Einfrieren in die Studien-App sind diese Texte die Quelle für `explanation.group_a`/`group_b`.
- **Im Code erzwungen (2026-06-22):** Der LLM-Prompt (`explain_agent._call_ollama`) erhält jetzt die Änderungsbedingung (A-`options`) und fordert ihren Schwellenwert an; `study_faithfulness.py` prüft, dass das eingefrorene B mindestens einen A-Schwellenwert trägt. Vollständiges Prompt-/Ollama-Referenzblatt: [2024f_system_prompts.md](../2024f_system_prompts.md).
- **Erledigt (2026-06-22):** Die sieben SoC-Band-`decision_codes` sind in `src/explain/decision_codes.py` registriert (`STOP_R1_SOC_RESERVE_STOP`, `NOOP_R1_SOC_HOLD`, `NOOP_R1_SOC_HOLD_PV`, `THROTTLE_R1_ECO`, `START_R1_STANDARD`, `START_R1_SUPER`, `NOOP_NIGHT_BLOCK`).
- **Erledigt (2026-06-22):** Nachtsperre im Kern (`night_block_enabled`, opt-in); R3-Safety und Reserve-Stop behalten Vorrang. **22:00** liefert verifiziert `NOOP_NIGHT_BLOCK`.
- **Offen:** Die in Text A 11:30/13:00 genannten Modus-Rückfälle („unter 75 % auf Eco", „unter 85 % auf Standard") setzen die Modus-Hysterese voraus, die noch in den Kern muss: `soc_standard_stop_pct`/`soc_super_stop_pct` stehen in der Config, werden von `r1_soc_band` aber noch nicht gelesen (real fällt die Stufe schon an der Bandgrenze 80 % bzw. 90 %). Bis die Hysterese im Kern ist, sind diese beiden Sätze leicht überstimmt; vor dem Studien-Freeze angleichen.
- **R4 geklärt (2026-06-23):** R4 ist im SoC-Band-Modus jetzt ein **opt-in** Veto (`forecast_veto_enabled`, Default **aus**), das **nur den PV-abhängigen Eco-Frischstart** betrifft (Quelle: lokale, deterministische Klarhimmel-Prognose `src/adapters/solar_forecast.py`, ADR 011). Die 13 Snapshots laufen mit `forecast_veto_enabled=False` und sind unberührt; **17:30** (`NOOP_R1_SOC_HOLD_PV`) hält ohnehin, weil die aktuelle PV unter `pv_start` liegt — die Band-Regel hält dort, der Frischstart kommt gar nicht erst zustande, also greift R4 nicht. Reihenfolge im Kern: `R3 > R2 > R5 > R4 > R1`. Ein **eigenes R4-Szenario** (Frischstart bei aktueller PV ≥ 6 kW, aber Klarhimmel-Prognose < Schwelle → `NOOP_R4_FORECAST_PV_INSUFFICIENT`) ist jetzt darstellbar, aber noch **nicht** als A/B-Zeile aufgenommen (offene Studien-Entscheidung).
- **Offen:** Der „Eco-Only-Testmodus" (Standard & Super gesperrt) ist als Szenario noch nicht abgebildet.
- **06:00 = 21 % (real geerdet, 2026-06-22):** auf den echten morgendlichen Tiefpunkt korrigiert (HA-Recorder Sommer 2026-05-24..06-03: SoC real 5–100 %, nachts bis nahe 0; 52 % wurde morgens nie beobachtet). Bei 21 % liegt der Akku unter der Hausreserve, daher Reserve-Stopp statt Halteband; das Halteband (`NOOP_R1_SOC_HOLD`) bleibt bei 20:00 abgebildet.
- **Realismus-Hinweis (Sommerdaten 2026-05-24..06-03, korrigiert 2026-06-23):** Der reale 10-kWh-Speicher erreicht im Sommer fast täglich 99–100 % SoC; die Standard- (≥ 80 %) und Super-Bänder (≥ 90 %) werden im Realbetrieb also **regelmäßig durchlaufen**, die SoC-Werte der Snapshots sind real geerdet (frühere Notiz „real noch nicht erreicht / max 75 %" war ein Artefakt des März-Fensters mit schwacher PV und vor dem 21.05. lückenhaften Sensoren). Didaktisch bleibt allein die *Anordnung* entlang eines Tages, an einem echten Verlauf verteilen sich die Bänder anders über die Zeit, und `THROTTLE_R1_ECO` tritt am 10-kWh-System eher als Abend-Rückfall denn als Morgen-Frischstart auf. Einzig die Chip-Temperatur 113 °C (13:30) ist konstruiert: real blieb der Miner ≤ 75 °C.
